x-logging: &logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  db:
    image: postgres:17
    logging: *logging
    environment:
      POSTGRES_DB: knt
      POSTGRES_USER: knt
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U knt"]
      interval: 3s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:8-alpine
    logging: *logging
    # Шина событий, а не хранилище: терять при перезапуске нечего, запись на диск выключена.
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  web:
    logging: *logging
    build:
      context: .
      args:
        PROXY: ${PROXY:-}   # из .env; пусто = качать напрямую
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "127.0.0.1:8001:8000"   # хост-порт 8001 (рядом с основным сайтом), nginx проксирует
    volumes:
      - ./media:/app/media       # bind-mount: nginx отдаёт /media с хоста
    restart: unless-stopped

  bot:
    logging: *logging
    build:
      context: .
      args:
        PROXY: ${PROXY:-}
    entrypoint: ["python", "manage.py", "bot"]
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
