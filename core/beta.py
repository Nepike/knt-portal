"""Бета-режим: пара страниц, которых наружу пока показывать нечего.

Сайт переехал на боевой домен раньше, чем были проверены все разделы. Проверка прошла,
и закрытой остаётся геймификация: профиль со всем, что к нему прицеплено, и магазин.
Страницы сделаны и работают, но открывать их решено не по частям, а разом, когда будут
готовы ещё кейсы и значки. Плашка «бета» в шапке — про сайт целиком, с этим списком
она не связана.

`CLOSED` — список ЗАПРЕЩЁННОГО, и это сознательный разворот. Начинали наоборот, со списка
разрешённого: пока закрыта была половина сайта, забыть внести раздел в белый список было
не страшно (заметно в тот же день), а забыть внести в чёрный — значило выставить наружу
непроверенное и не узнать. Теперь соотношение обратное: в белом списке пришлось бы держать
160 имён ради одного закрытого, и цена забытого имени сменилась на противоположную —
работающий раздел молча отвечал бы 403 на бою. Когда закрытых страниц снова станет много,
разворачивать обратно.

Имена урлов, а не пути: путь можно поменять, а имя живёт в шаблонах и в `reverse`,
и разойтись они не могут.
"""

from django.conf import settings

CLOSED = {
    # Профиль целиком, вместе со своими действиями: правкой, закрытием сессий и
    # экипировкой. Открывать их поодиночке смысла нет — все они живут на одной странице.
    "profile",
    "profile_edit",
    "session_end",
    "item_equip",
    "item_unequip",
    "wallet",
    # Магазин — та же геймификация: покупать, не видя купленного в профиле, нечего.
    "shop",
    "item_card",
    "item_buy",
    # Лекторий: раздел готов целиком — и сдача записей, и очередь выпечки, — но курсов
    # в нём ещё нет. Открывать пустую витрину незачем: открываем, когда появятся первые
    # записи. `hls_check` сюда не нужен: он и так только для сотрудников.
    "playlist_list",
    "playlist_detail",
    "playlist_new",
    "playlist_edit",
    "lecture_add",
    "playlist_review",
}


def locked(request):
    """Закрыт ли текущий адрес для этого пользователя."""
    if not settings.BETA:
        return False
    if request.user.is_authenticated and request.user.is_staff:
        return False
    match = request.resolver_match
    return bool(match) and match.url_name in CLOSED
